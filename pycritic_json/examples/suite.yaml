- est:
    mark: 1
    comment: "Test"
  cond:
    - arg: var1

    # comparison functions: lt, le, gt, ge, eq, ne
    - func: lt
      arg: var2
      example: 5.2

- est:
    mark: 1
  cond:
    - func: eq
      arg: var4
      example: 5
  
- est:
    mark: 1
    comment: "'or' operator analogue"
  cond:
    # fit   -> (min, max)
    # nfit  -> [min, max)
    # xfit  -> (min, max]
    # nxfit -> [min, max]
    - func: fit
      arg: var3
      min: 6
      max: 10
    - func: regex
      arg: var10
      pattern: "^some pattern$"
